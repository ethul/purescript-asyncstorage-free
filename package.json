{
  "name": "purescript-asyncstorage-free",
  "version": "3.1.1",
  "private": true,
  "scripts": {
    "purs:compile": "purs compile 'src/**/*.purs' 'test/**/*.purs' 'bower_components/purescript-*/src/**/*.purs'",
    "pscid": "pscid --output-directory output",
    "test": "node -e 'require(\"./output/Test.Main\").main()'"
  },
  "license": "MIT"
}
